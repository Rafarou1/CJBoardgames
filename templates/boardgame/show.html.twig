{% extends 'base.html.twig' %}

{% block title %}Boardgame{% endblock %}

{% block main %}
    <h1>Boardgame</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ boardgame.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ boardgame.name }}</td>
            </tr>
            <tr>
                <th>Difficulty</th>
                <td>{{ boardgame.difficulty }}</td>
            </tr>
            <tr>
                <th>Year</th>
                <td>{{ boardgame.year }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_boardgame_index') }}">back to list</a>

    <a href="{{ path('app_boardgame_edit', {'id': boardgame.id}) }}">edit</a>

    {{ include('boardgame/_delete_form.html.twig') }}
{% endblock %}
