{% extends 'base.html.twig' %}

{% block title %}Armoire{% endblock %}

{% block main %}
    <h1>Armoire</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ armoire.id }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ armoire.description }}</td>
            </tr>
            <tr>
                <th>Published</th>
                <td>{{ armoire.published ? 'Yes' : 'No' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_armoire_index') }}">back to list</a>

    <a href="{{ path('app_armoire_edit', {'id': armoire.id}) }}">edit</a>

    {{ include('armoire/_delete_form.html.twig') }}

{% endblock %}
