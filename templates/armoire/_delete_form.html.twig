<form method="post" action="{{ path('app_armoire_delete', {'id': armoire.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ armoire.id) }}">
    <button class="btn">Delete</button>
</form>
